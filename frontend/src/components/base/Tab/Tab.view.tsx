import React from 'react';
import { TabProps } from './Tab.props';
import { Container } from './Tab.style';

const Tab = (props: TabProps): JSX.Element => {
  return <Container> Tab </Container>;
};

export default React.memo(Tab);
