import React from 'react';

import SigninView from './Signin.view';

const Signin = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <SigninView {...generatedProps} />;
};

export default Signin;
