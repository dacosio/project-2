import React from 'react';

import SignupView from './Signup.view';

const Signup = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <SignupView {...generatedProps} />;
};

export default Signup;
