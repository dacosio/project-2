import React from 'react';

import ContactPageView from './ContactPage.view';

const ContactPage = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <ContactPageView {...generatedProps} />;
};

export default ContactPage;
