import React from 'react';

import JbResearchView from './JbResearch.view';

const JbResearch = (): JSX.Element => {
  const generatedProps = {
    // generated props here
  };
  return <JbResearchView {...generatedProps} />;
};

export default JbResearch;
